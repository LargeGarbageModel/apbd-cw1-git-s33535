The program allows various basic operations on numbers.

The conflict when trying to merge the 'feature-max' will be resolved by removing the changes from Program.cs.

This line was added purely to have a commit on main for Ex 6